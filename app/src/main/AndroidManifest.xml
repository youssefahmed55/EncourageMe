<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.encourageme">

    <application
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:icon="@mipmap/encourage"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/encourage"
        android:supportsRtl="true"
        android:theme="@style/Theme.EncourageMe">
        <activity
            android:name="com.encourageme.Splash"
            android:configChanges="orientation"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.encourageme.MainActivity"
            android:exported="true"

            ></activity>
        <activity
            android:name="com.encourageme.Intro"
            android:configChanges="orientation"
            android:exported="false"

            ></activity>


        <receiver android:name="com.encourageme.ReminderBroadcast" />
    </application>

</manifest>